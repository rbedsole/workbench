const seed={systems:[{id:"baseline",name:"Core Experience",desc:"The rules that define how Delta Emerald should feel.",decisions:["modern","simple"]},{id:"traversal",name:"Traversal",desc:"How the player moves through Hoenn with Pokémon.",decisions:["slots","auto"]},{id:"encounters",name:"Encounters & Catching",desc:"Wild encounters, followers and catching behavior.",decisions:["hordes","throw"]},{id:"shiny",name:"Shiny System",desc:"Visibility, odds and notification behavior.",decisions:["visible","odds"]}],decisions:[{id:"modern",system:"baseline",title:"Modern mechanics baseline",status:"Decided",text:"Use modern Pokémon mechanics by default while offering classic-style options where practical.",affects:["simple","hordes","throw"]},{id:"simple",system:"baseline",title:"Keep the single-player experience focused",status:"Decided",text:"Prefer simple, coherent systems over piling on mechanics just because they are possible.",affects:["slots","hordes"]},{id:"slots",system:"traversal",title:"Three traversal Pokémon slots",status:"Decided",text:"Use designated flying, climbing and swimming slots with eligible Pokémon filtered for each role.",affects:["auto"]},{id:"auto",system:"traversal",title:"Automatic traversal selection",status:"Decided",text:"When traversal is required, automatically use the Pokémon assigned to the appropriate traversal slot.",affects:[]},{id:"hordes",system:"encounters",title:"Hordes remain grass encounters",status:"Decided",text:"Horde encounters are tied to grass encounters rather than appearing as overworld groups.",affects:[]},{id:"throw",system:"encounters",title:"Overworld surprise throw",status:"Exploring",text:"Throwing a Poké Ball at an unaware wild Pokémon gives an increased capture chance on the first turn.",affects:["simple"]},{id:"visible",system:"shiny",title:"Shinies visible in the overworld",status:"Decided",text:"Shiny Pokémon are visibly shiny before battle and can trigger configurable notifications.",affects:["odds"]},{id:"odds",system:"shiny",title:"Configurable shiny odds",status:"Decided",text:"Offer shiny odds from 1/256 through 1/8192, with 1/4096 as the default.",affects:[]}]};
let data=JSON.parse(localStorage.getItem("workbench-data")||"null")||structuredClone(seed);let current=null;
const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);function save(){localStorage.setItem("workbench-data",JSON.stringify(data))}
function show(id){$$(".view").forEach(v=>v.classList.remove("active"));$("#"+id).classList.add("active");scrollTo(0,0)}
function counts(){$("#systemCount").textContent=data.systems.length+" systems";$("#decisionCount").textContent=data.decisions.length+" decisions"}
function render(){counts();$("#systemsPanel").innerHTML=data.systems.map(s=>{let ds=data.decisions.filter(d=>d.system===s.id);return '<button class="system-card" data-system="'+s.id+'"><strong>'+s.name+'</strong><small>'+s.desc+'</small><div class="system-meta">'+ds.length+' decisions · '+ds.filter(d=>d.status==="Decided").length+' decided</div></button>'}).join("");
$("#decisionsPanel").innerHTML=data.decisions.map(card).join("");let impacted=data.decisions.filter(d=>d.affects.length);$("#impactPanel").innerHTML=impacted.map(d=>'<button class="impact-card" data-decision="'+d.id+'"><strong>'+d.title+'</strong><p>Changing this may affect '+d.affects.length+' decision'+(d.affects.length>1?'s':'')+': '+d.affects.map(x=>get(x).title).join(", ")+'</p></button>').join("")||'<div class="empty">No dependencies yet.</div>';bindCards()}
function get(id){return data.decisions.find(d=>d.id===id)}function card(d){let c=d.status==="Exploring"?"exploring":d.status==="Needs Decision"?"needs":"";return '<button class="decision-card" data-decision="'+d.id+'"><span class="dot '+c+'"></span><div><strong>'+d.title+'</strong><small>'+data.systems.find(s=>s.id===d.system).name+'</small><span class="status">'+d.status+'</span></div></button>'}
function bindCards(){$$("[data-decision]").forEach(b=>b.onclick=()=>detail(b.dataset.decision));$$("[data-system]").forEach(b=>b.onclick=()=>{let s=data.systems.find(x=>x.id===b.dataset.system);$("#systemsPanel").innerHTML='<button class="back" id="allSystems">‹ All systems</button><h2>'+s.name+'</h2><p style="color:#9aa7c1;margin:7px 0 18px">'+s.desc+'</p>'+data.decisions.filter(d=>d.system===s.id).map(card).join("");$("#allSystems").onclick=render;bindCards()})}
function detail(id){current=id;let d=get(id),deps=d.affects.map(get),incoming=data.decisions.filter(x=>x.affects.includes(id));$("#detailBody").innerHTML='<span class="eyebrow">'+data.systems.find(s=>s.id===d.system).name.toUpperCase()+'</span><h2 style="margin-top:7px">'+d.title+'</h2><div class="detail-card"><label>Status</label><select id="status"><option>Decided</option><option>Exploring</option><option>Needs Decision</option><option>Superseded</option></select><label>Decision</label><textarea id="decisionText"></textarea><button class="primary" id="saveDecision">Save change</button></div><div class="detail-card"><h3>Dependencies</h3>'+(deps.length?deps.map(x=>'<div class="impact-card"><strong>→ '+x.title+'</strong><p>May be affected if this decision changes.</p></div>').join(""):'<p class="empty">This decision currently affects nothing else.</p>')+(incoming.length?'<h3 style="margin-top:18px">Depends on this</h3>'+incoming.map(x=>'<div class="impact-card"><strong>← '+x.title+'</strong></div>').join(""):'')+'</div>';$("#status").value=d.status;$("#decisionText").value=d.text;$("#saveDecision").onclick=()=>{let old=d.text;d.status=$("#status").value;d.text=$("#decisionText").value.trim();save();render();toast(old!==d.text&&d.affects.length?"Saved · "+d.affects.length+" related decision"+(d.affects.length>1?"s":"")+" may be affected":"Saved");detail(id)};show("detail")}
function toast(t){let e=$("#toast");e.textContent=t;e.classList.add("show");setTimeout(()=>e.classList.remove("show"),2200)}
$$("[data-go]").forEach(b=>b.onclick=()=>show(b.dataset.go));$(".project-card").onclick=()=>{render();show("project")};$$(".tab").forEach(b=>b.onclick=()=>{$$(".tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");$$(".panel").forEach(x=>x.classList.add("hidden"));$("#"+b.dataset.tab+"Panel").classList.remove("hidden")});$("#resetBtn").onclick=()=>{data=structuredClone(seed);save();render();toast("Demo data reset")};render();